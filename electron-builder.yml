appId: pro.getpeakflow.core
productName: PeakFlow
directories:
  buildResources: resources
  output: release
files:
  - '!**/.vscode/*'
  - '!artifacts/**'
  - '!prototypes/**'
  - '!screenshots/**'
  - '!docs/**'
  - '!src/*'
  - '!electron.vite.config.*'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  - '!sidecar/*'
win:
  target:
    - target: nsis
      arch:
        - x64
  icon: resources/icon.ico
  requestedExecutionLevel: asInvoker
  artifactName: PeakFlow-Setup-${version}.${ext}
  # Avoid winCodeSign cache extract (needs symlink privilege on Windows without Dev Mode)
  signAndEditExecutable: false
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  installerIcon: resources/icon.ico
  createDesktopShortcut: true
  createStartMenuShortcut: true
extraResources:
  - from: resources/soundsplit-bridge.ps1
    to: soundsplit-bridge.ps1
  - from: resources/icon.png
    to: icon.png
  - from: resources/tray-icon.png
    to: tray-icon.png
asarUnpack:
  - "**/*.node"
  - "node_modules/koffi/**/*"
publish:
  provider: github
  owner: inchwormz
  repo: peakflow-releases
